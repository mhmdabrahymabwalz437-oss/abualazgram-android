# تقرير تنفيذ ABUALAZGRAM

## النتيجة الحالية

تم إنشاء نسخة عمل مستقلة من مستودع Telegram Android الرسمي في هذا المجلد. جرى الحفاظ على المصدر الأصلي ووحداته الفرعية، مع تطبيق تغييرات أولية على الهوية والإعدادات الحساسة. لم يُنشأ عميل مراسلة وهمي من الصفر.

| المجال | ما نُفّذ | الحالة |
|---|---|---|
| المصدر | استنساخ DrKLO/Telegram مع الوحدات الفرعية | مكتمل |
| الاسم الظاهر | ABUALAZGRAM وABUALAZGRAM Beta في المورد الأساسي | مكتمل |
| معرف التطبيق | `com.abualaz.abualazgram` في `gradle.properties` | مكتمل |
| الأيقونة | استبدال أيقونات launcher بالصورة الجديدة 112307.jpg وتحويلها إلى كثافات Android الأساسية | مكتمل |
| مفاتيح API | إزالة القيم الرسمية ووضع `APP_ID=0` و`APP_HASH=""` | مكتمل؛ يتطلب بيانات المطوّر |
| فحص التحديثات | تعطيل رابط/فحص متجر Telegram الرسمي | مكتمل |
| Passkeys | تعطيلها لأن المصدر يقيّدها بمعرّفات رسمية | مكتمل |
| Google Services | إزالة الملفات الرسمية التي تطابق حزم Telegram الرسمية | مكتمل؛ يتطلب ملفًا خاصًا عند الحاجة |
| About | توثيق هوية المطوّر في README؛ إضافة شاشة داخلية مخصصة تحتاج متابعة واجهة | جزئي |
| الصورة الشخصية | لم تُدمج في الملف الشخصي لأن ملف صورة شخصية منفصل لم يُرفق | معلّق |
| APK/AAB | لم يُنتج بعد بسبب غياب Android SDK/NDK وبيانات API/Firebase | معلّق |

## ميزات KRGram

الميزات التي تغيّر المظهر أو تضيف حماية محلية، مثل الثيم الداكن/الفاتح وتحسينات الطمس وإخفاء المحادثات بقفل محلي، يمكن تنفيذها تدريجيًا بعد تثبيت نسخة البناء الأساسية. لم تُنفّذ الميزات التي تتجاوز حماية Telegram أو قيود الخادم، ومنها تعطيل مؤقت التدمير الذاتي، حفظ المحتوى المقيد، إجبار لقطات الشاشة أو التسجيل، Ghost Mode، استعادة الرسائل المحذوفة، أو إظهار محتوى بعد الطرد/الحظر. هذه ليست مجرد مفاتيح واجهة؛ تنفيذها قد يضر بالخصوصية أو يخالف شروط الاستخدام.

## الملفات الجديدة والمهمة

تمت إضافة `README.md` مخصص للمشروع، و`FEATURE_POLICY.md`، و`assets/abualazgram-icon-master.png` كنسخة مربعة من الصورة الجديدة، و`tools_make_icon_v2.py` كسكربت قابل لإعادة التشغيل لتوليد الأيقونات. تم تعديل `gradle.properties` و`BuildVars.java` و`UserConfig.java` و`SettingsActivity.java` و`AbualazgramSettings.java` وموارد الألوان والانتقال وملفات PNG الخاصة بأيقونات launcher. أزيلت ملفات Google Services الرسمية من وحدات المشروع.

## متطلبات المتابعة

يلزم تزويد المشروع بـ API ID وAPI Hash خاصين بالمطوّر، وملف `google-services.json` الخاص بالمعرف الجديد عند الحاجة إلى Firebase، وشهادة توقيع release. كما يلزم إرفاق الصورة الشخصية الأصلية إذا كان المطلوب وضعها في شاشة البداية وصفحة الملف الشخصي دون تغيير ملامحها.

## التحقق

ينبغي تشغيل `./gradlew tasks --all` ثم مهمة build المناسبة بعد تثبيت Android Studio 2025.1.4 وSDK 36 وNDK 27.2.12479018. لا ينبغي نشر أي APK قبل مراجعة التراخيص، وإشعارات المصدر، وملفات الاعتماد، وسياسة الخصوصية، والتأكد من أن التطبيق يوضح بجلاء أنه عميل غير رسمي.

## نتيجة اختبار البناء

تم تشغيل مهمة `:TMessagesProj_AppStandalone:assembleAfatDebug`. نجحت تهيئة `buildSrc` باستخدام JDK 21، ثم توقف البناء عند تهيئة مشروع Android لأن البيئة لا تحتوي على Android SDK أو متغير `ANDROID_HOME`/`ANDROID_SDK_ROOT`. لذلك لم يُنتج APK أو AAB داخل البيئة الحالية. بعد تثبيت SDK 36 وNDK 27.2.12479018 وإعداد `local.properties` يمكن إعادة تشغيل المهمة نفسها.

## التوسعة الأخيرة

أضيفت `AbualazgramSettings.java` لتوفير تخزين محلي منظم لإعدادات Luxury Mode ولوحة الألوان ونمط الأداء وقفل التطبيق وإخفاء المحادثات. وأضيف صف تفاعلي باسم Luxury Mode إلى `SettingsActivity.java` يبدّل الإعداد محليًا ويحدّث القائمة. كما أضيفت موارد ألوان فاخرة وانتقال fade خفيف، وتسميات الموارد اللازمة، ووثيقة `FEATURE_POLICY.md`.

رُفع `UserConfig.MAX_ACCOUNT_DEFAULT_COUNT` و`UserConfig.MAX_ACCOUNT_COUNT` إلى 10، وأصبح `getMaxAccountCount()` يعيد السعة الجديدة. يستخدم المصدر مصفوفات ثابتة متعددة الوحدات، ولذلك يلزم اختبار ضغط وذاكرة وترحيل الحسابات قبل إصدار إنتاجي.

## محاولة البناء الأخيرة

تم تثبيت Android SDK Platform 36 وBuild Tools 36.0.0 وNDK 27.2.12479018 وCMake 3.22.1، وإضافة `local.properties` للمسار المحلي. بدأت مهمة `:TMessagesProj_AppStandalone:assembleAfatDebug` بالمفاتيح المضمّنة، لكنها دخلت في تجميع مكتبات CMake لجميع المعماريات واستمرت عدة دقائق مع ضغط ذاكرة مرتفع؛ أُوقفت قبل اكتمالها لحماية البيئة، ولم ينتج APK. لا تزال المفاتيح موجودة داخل `BuildVars.java` كما طلب المستخدم.
